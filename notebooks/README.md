# Notebooks

Notebooks for data exploration and visualization.
1 preps RDS files from raw data.
2 preps lists for Stan.
3 runs a simple 2-level exact GPR for one specific UN subregion at a time.
4 runs a 3-level exact GPR, which increases runtime significantly, therefore:
5 explores Hilbert space methods for approximating the GPR instead.
